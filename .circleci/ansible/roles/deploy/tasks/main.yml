---
- name: Copy artifact.tar.gz to EC2
  template:
    src: "/project/"
    dest: "~/"

- name: "Extract Artifact"
  shell: |
    tar -xvfz artifact.tar.gz

- name: Executing node
  shell: |
    npm install
    pm2 stop default
    pm2 start npm -- start

