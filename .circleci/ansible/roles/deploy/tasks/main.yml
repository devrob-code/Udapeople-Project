---
- name: Copy index test page
  template:
    src: "project/artifact.tar.gz"
    dest: "/"

- name: "Extract Artifact"
  shell: |
    tar -xvfz artifact.tar.gz

- name: Executing node
  shell: |
    npm install
    pm2 stop default
    pm2 start npm -- start

